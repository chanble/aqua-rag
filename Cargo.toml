[package]
name = "aqua-rag"
version = "0.1.0"
edition = "2021"
description = "Knowledge base management and vector retrieval for aquaDB"

[dependencies]
lancedb = { version = "0.27", features = ["rustls"] }  # rustls 用于更轻量的 TLS 支持
ort = "2.0"